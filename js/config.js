/**
 * Supabase configuration
 * 1. Create a project at https://supabase.com/dashboard
 * 2. Copy Project URL + anon public key into the values below
 * 3. Run supabase/schema.sql in the SQL Editor
 */
export const SUPABASE_URL = 'https://klnmphyscamehhoaudfn.supabase.co';
export const SUPABASE_ANON_KEY = 'sb_publishable_IHaKkecqmpIn3ILmAjbjlw_00T-2QyY';

/**
 * YOUR email — the first account with this email becomes Owner automatically
 * (only works when no owner exists yet). Change this to your real email!
 */
export const OWNER_EMAIL = 'sukiiipookie@gmail.com';

/** @deprecated — use Admin button in nav */
export const ADMIN_LOGO_CLICKS = 5;

/** Visiting #suki-admin opens login, #suki-register opens registration */
export const ADMIN_HASH = 'suki-admin';
export const REGISTER_HASH = 'suki-register';